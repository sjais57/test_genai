# config/api_keys/test_ges.yaml
id: test_ges
owner: Test User

metadata:
  ges_namespaces:
    "com.text.qwe.tert":
      group_claims:
        "ADS":
          test_claim: "ads_user"
          models: ["test-model"]
        "AD.read.ASW":
          test_claim: "read_user" 
          models: ["test-model-2"]

  default_claims:
    test_claim: "default_user"
    models: ["default-model"]
